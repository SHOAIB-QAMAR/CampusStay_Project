<nav class="navbar navbar-expand-md navbar-light bg-light sticky-top border-0">
    <div class="container-fluid">

        <a class="navbar-brand p-2" href="/listings">
            <i class="fa-solid fa-compass"></i>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse border-0" id="navbarNavAltMarkup">

            <div class="navbar-nav w-100 d-flex flex-column flex-md-row align-items-center align-items-md-stretch justify-content-between">

                <div class="nav-item order-2 order-md-1">
                    <a class="nav-link" href="/listings">Explore</a>
                </div>

                <div class="nav-item d-flex justify-content-center my-2 my-md-0 order-1 order-md-2">
                    <form class="d-flex" role="search" action="/listings/search" method="GET">
                        <input class="form-control me-2 search-input" type="search" name="q"
                            placeholder="Search destinations" aria-label="Search" />
                        <button class="btn search-btn" type="submit">
                            <i class="fa-solid fa-magnifying-glass"></i>Search
                        </button>
                    </form>
                </div>

                <div class="nav-item d-flex flex-column flex-md-row align-items-center gap-2 order-3">
                    
                    <% if(currUser){ %>
                        <!-- When user is logged in -->
                        <div class="nav-item d-flex align-items-center gap-3">
                            <a class="nav-link text-nowrap p-0" href="/become-host">Become a Host</a>
                            <span class="nav-link text-nowrap fw-bold p-0">
                                <%= currUser.role==='landlord' ? 'Landlord' : 'User' %>: <%= currUser.username %>
                            </span>
                            <a class="nav-link text-nowrap p-0" href="/logout">Log out</a>
                        </div>
                        <% } else { %>
                            <!-- When user is not logged in -->
                            <div class="nav-item d-flex align-items-center gap-3">
                                <a class="nav-link text-nowrap p-0" href="/become-host">Become a Host</a>
                                <a class="nav-link text-nowrap p-0" href="/signup"><b>Sign up</b></a>
                                <a class="nav-link text-nowrap p-0" href="/login"><b>Log in</b></a>
                            </div>
                            <% } %>

                    <!-- <a class="nav-link text-nowrap" href="/become-host">Become a Host</a>

                    <% if(!currUser){ %>
                        <a class="nav-link text-nowrap" href="/signup"><b>Sign up</b></a>
                        <a class="nav-link text-nowrap" href="/login"><b>Log in</b></a>
                        <% } %>

                            <% if(currUser){ %>
                                <a class="nav-link text-nowrap" href="/logout">Log out</a>
                                <% } %> -->
                </div>
                
            </div>
        </div>
    </div>
</nav>
